# 🚀 الإعداد النهائي والتشغيل

## ⚡ الحل الأسرع والأسهل

### الخيار 1: Laragon (موصى به بشدة - 5 دقائق فقط!)

1. **حمّل Laragon:**
   - اذهب إلى: https://laragon.org/download/
   - حمّل الإصدار الكامل (Full)
   - حجم الملف: ~200 MB

2. **ثبّت Laragon:**
   - شغّل الملف المحمّل
   - اتبع التعليمات (كل شيء افتراضي)
   - سيتم تثبيت: PHP, MySQL, Composer, Node.js تلقائياً!

3. **شغّل Laragon:**
   - افتح Laragon
   - اضغط "Start All"
   - كل شيء جاهز!

4. **شغّل المشروع:**
   ```bash
   cd C:\Users\b9di\Desktop\lw
   start.bat
   ```

5. **افتح المتصفح:**
   - http://localhost:8000

---

### الخيار 2: Docker (لا يحتاج تثبيت PHP محلياً)

1. **حمّل Docker Desktop:**
   - https://www.docker.com/products/docker-desktop/
   - ثبّت وشغّل Docker Desktop

2. **شغّل المشروع:**
   ```bash
   cd C:\Users\b9di\Desktop\lw
   docker-compose up -d
   ```

3. **إعداد Laravel:**
   ```bash
   docker-compose exec app php artisan key:generate
   docker-compose exec app php artisan migrate --seed
   docker-compose exec app npm run build
   ```

4. **افتح المتصفح:**
   - http://localhost:8000

---

## 📋 بعد التثبيت

### 1. إنشاء قاعدة البيانات

إذا استخدمت Laragon:
- قاعدة البيانات موجودة تلقائياً
- اسم قاعدة البيانات: `law_firm`
- المستخدم: `root`
- كلمة المرور: (فارغة)

إذا استخدمت Docker:
- قاعدة البيانات موجودة تلقائياً
- الإعدادات في `docker-compose.yml`

### 2. تعديل ملف .env

افتح ملف `.env` وأضف/عدّل:

```env
DB_DATABASE=law_firm
DB_USERNAME=root
DB_PASSWORD=
```

(اترك كلمة المرور فارغة إذا استخدمت Laragon)

### 3. تشغيل المشروع

```bash
# إذا استخدمت Laragon
start.bat

# أو يدوياً
php artisan migrate --seed
php artisan serve
```

---

## 🔑 بيانات الدخول

### لوحة الإدارة
- **البريد:** admin@lawfirm.sa
- **كلمة المرور:** password

### لوحة العميل
- **رقم الهوية:** 1234567890
- **رقم القضية:** CASE-2024-001

---

## ✅ التحقق من التثبيت

بعد تشغيل `start.bat` أو `php artisan serve`، يجب أن ترى:

```
INFO  Server running on [http://127.0.0.1:8000]
```

افتح المتصفح وانتقل إلى العنوان أعلاه.

---

## 🆘 حل المشاكل

### PHP غير موجود
- استخدم Laragon (يتضمن كل شيء)
- أو Docker (لا يحتاج PHP محلياً)

### Composer غير موجود
- Laragon يتضمن Composer تلقائياً
- أو ثبّته من: https://getcomposer.org/download/

### Node.js غير موجود
- Laragon يتضمن Node.js
- أو ثبّته من: https://nodejs.org/

### قاعدة البيانات
- تأكد من تشغيل MySQL في Laragon
- أو استخدم Docker (MySQL يعمل تلقائياً)

---

## 💡 نصيحة

**استخدم Laragon** - أسهل وأسرع حل! يتضمن كل شيء جاهزاً.

