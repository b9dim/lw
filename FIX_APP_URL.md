# 🔧 إصلاح APP_URL في Render Dashboard

## المشكلة
`APP_URL` معين على `http://` بدلاً من `https://` في Render Dashboard.

## الحل السريع

### الخطوات:

1. **افتح Render Dashboard**
   - اذهب إلى: https://dashboard.render.com
   - سجل الدخول إلى حسابك

2. **اختر الخدمة**
   - من القائمة الجانبية، اختر **"Services"**
   - اختر خدمة `law-firm-app` (أو اسم خدمتك)

3. **افتح Environment Variables**
   - اضغط على تبويب **"Environment"** في أعلى الصفحة
   - أو اضغط على **"Environment"** من القائمة الجانبية

4. **ابحث عن APP_URL**
   - ابحث عن المتغير `APP_URL` في القائمة
   - أو استخدم البحث (Ctrl+F / Cmd+F)

5. **عدّل القيمة**
   - اضغط على أيقونة **"Edit"** بجانب `APP_URL`
   - أو اضغط على القيمة نفسها لتعديلها

6. **غيّر من HTTP إلى HTTPS**
   - **قبل:** `http://lw-2uez.onrender.com`
   - **بعد:** `https://lw-2uez.onrender.com`
   
   ⚠️ **مهم:**
   - يجب أن يبدأ بـ `https://`
   - بدون `/` في النهاية
   - استبدل `lw-2uez.onrender.com` باسم نطاقك الفعلي

7. **احفظ التغييرات**
   - اضغط على **"Save Changes"** أو **"Update"**
   - سيتم إعادة تشغيل الخدمة تلقائياً

8. **انتظر إعادة التشغيل**
   - انتظر حتى يكتمل إعادة التشغيل (عادة 1-2 دقيقة)
   - يمكنك متابعة التقدم من تبويب **"Logs"**

## مثال على القيمة الصحيحة:

```
https://lw-2uez.onrender.com
```

## مثال على القيمة الخاطئة:

```
http://lw-2uez.onrender.com  ❌
https://lw-2uez.onrender.com/  ❌ (يوجد / في النهاية)
```

## التحقق من التغيير:

بعد إعادة التشغيل:

1. افتح الموقع في المتصفح
2. اضغط `F12` لفتح Developer Tools
3. اذهب إلى تبويب **"Application"** > **"Cookies"**
4. تحقق من أن جميع الـ cookies لديها:
   - ✅ **Secure** = true
   - ✅ **Domain** = your-domain.onrender.com

5. في Console، يجب ألا ترى أي تحذيرات حول:
   - "A cookie was not sent to an insecure origin"
   - "Mixed content"

## إذا لم تجد APP_URL:

إذا لم يكن `APP_URL` موجوداً في Environment Variables:

1. اضغط على **"Add Environment Variable"** أو **"+"**
2. **Key:** `APP_URL`
3. **Value:** `https://your-app-name.onrender.com`
4. احفظ التغييرات

## ملاحظات إضافية:

- ⚠️ **لا تنسى:** بعد تغيير `APP_URL`، سيتم إعادة تشغيل الخدمة تلقائياً
- ✅ **تأكد:** من أن النطاق صحيح (بدون `/` في النهاية)
- 🔒 **الأمان:** HTTPS مطلوب لجميع الـ cookies والجلسات

---

✅ **بعد تطبيق هذا التغيير، يجب أن تختفي جميع مشاكل HTTPS!**

