# ✅ تم تثبيت مكتبات PHP بنجاح!

## الخطوات التالية:

### 1. ✅ تم: تثبيت مكتبات PHP
   - تم بنجاح!

### 2. تثبيت مكتبات JavaScript

**في Terminal الذي يعمل فيه Node.js (CMD أو Git Bash):**

```bash
cd C:\Users\b9di\Desktop\lw
npm install
```

أو إذا كان npm غير موجود في PATH، استخدم المسار الكامل:
```bash
C:\Program Files\nodejs\npm.cmd install
```

### 3. ✅ تم: توليد مفتاح التطبيق
   - تم بنجاح!

### 4. إنشاء قاعدة البيانات

1. افتح MySQL (من Laragon أو XAMPP)
2. أنشئ قاعدة بيانات جديدة:
   ```sql
   CREATE DATABASE law_firm;
   ```
3. تأكد من أن ملف `.env` يحتوي على:
   ```env
   DB_DATABASE=law_firm
   DB_USERNAME=root
   DB_PASSWORD=
   ```

### 5. تشغيل Migrations

```bash
php artisan migrate --seed
```

هذا الأمر سينشئ جميع الجداول ويملأها ببيانات تجريبية.

### 6. بناء الأصول

```bash
npm run build
```

أو للتطوير (مع Hot Reload):
```bash
npm run dev
```

### 7. تشغيل المشروع

```bash
php artisan serve
```

---

## 🌐 الوصول للمشروع

بعد تشغيل `php artisan serve`، افتح المتصفح على:

**http://localhost:8000**

---

## 🔑 بيانات الدخول

### لوحة الإدارة
- **البريد:** admin@lawfirm.sa
- **كلمة المرور:** password

### لوحة العميل
- **رقم الهوية:** 1234567890
- **رقم القضية:** CASE-2024-001

---

## ⚡ تشغيل سريع (كل شيء في أمر واحد)

```bash
npm install && php artisan migrate --seed && npm run build && php artisan serve
```

---

## 📝 ملاحظات

- تأكد من تشغيل MySQL قبل تشغيل `migrate`
- إذا كان npm غير موجود، أعد فتح Terminal بعد تثبيت Node.js
- ملف `.env` جاهز ومفتاح التطبيق تم توليده

