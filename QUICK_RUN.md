# 🚀 تشغيل سريع

## ✅ تم إنشاء ملف .env بنجاح!

## الخطوات التالية:

### 1. تثبيت المكتبات (إذا لم تكن مثبتة)

```bash
# تثبيت مكتبات PHP
composer install

# أو إذا كان composer.phar موجود
php composer.phar install

# تثبيت مكتبات JavaScript
npm install
```

### 2. توليد مفتاح التطبيق

```bash
php artisan key:generate
```

### 3. إنشاء قاعدة البيانات

1. افتح MySQL (من Laragon أو XAMPP)
2. أنشئ قاعدة بيانات باسم: `law_firm`
3. تأكد من أن ملف `.env` يحتوي على:
   ```env
   DB_DATABASE=law_firm
   DB_USERNAME=root
   DB_PASSWORD=
   ```

### 4. تشغيل Migrations

```bash
php artisan migrate --seed
```

### 5. بناء الأصول

```bash
npm run build
```

أو للتطوير:
```bash
npm run dev
```

### 6. تشغيل المشروع

```bash
php artisan serve
```

أو ببساطة:
```bash
start.bat
```

---

## 🌐 الوصول للمشروع

بعد تشغيل `php artisan serve`، افتح المتصفح على:

**http://localhost:8000**

---

## 🔑 بيانات الدخول

### لوحة الإدارة
- البريد: `admin@lawfirm.sa`
- كلمة المرور: `password`

### لوحة العميل
- رقم الهوية: `1234567890`
- رقم القضية: `CASE-2024-001`

---

## ⚡ تشغيل سريع (كل شيء في أمر واحد)

```bash
composer install && npm install && php artisan key:generate && php artisan migrate --seed && npm run build && php artisan serve
```

---

## 📝 ملاحظات

- تأكد من تشغيل MySQL قبل تشغيل `migrate`
- إذا كان Composer غير موجود في PATH، استخدم `php composer.phar` بدلاً من `composer`
- ملف `.env` جاهز الآن ويمكنك تعديله حسب حاجتك

