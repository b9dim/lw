# دليل البدء السريع

## ⚡ تشغيل سريع

### الخطوة 1: تثبيت المتطلبات

```bash
# تثبيت مكتبات PHP
composer install

# تثبيت مكتبات JavaScript  
npm install
```

### الخطوة 2: إعداد قاعدة البيانات

1. أنشئ قاعدة بيانات MySQL باسم `law_firm`
2. عدّل ملف `.env` وأضف بيانات قاعدة البيانات:

```env
DB_DATABASE=law_firm
DB_USERNAME=root
DB_PASSWORD=your_password
```

### الخطوة 3: إعداد المشروع

```bash
# توليد مفتاح التطبيق
php artisan key:generate

# إنشاء الجداول وملء البيانات التجريبية
php artisan migrate --seed
```

### الخطوة 4: بناء الأصول

```bash
# للتطوير (يفضل)
npm run dev

# أو للإنتاج
npm run build
```

### الخطوة 5: تشغيل المشروع

```bash
php artisan serve
```

افتح المتصفح على: **http://localhost:8000**

---

## 🔑 بيانات الدخول

### لوحة الإدارة
- البريد: `admin@lawfirm.sa`
- كلمة المرور: `password`

### لوحة العميل
- رقم الهوية: `1234567890`
- رقم القضية: `CASE-2024-001`

---

## 📝 ملاحظات مهمة

1. **PHP غير مثبت؟**
   - استخدم XAMPP أو WAMP
   - أو ثبّت PHP من php.net

2. **Composer غير مثبت؟**
   - حمّل من getcomposer.org

3. **قاعدة البيانات؟**
   - تأكد من تشغيل MySQL
   - أنشئ قاعدة بيانات `law_firm`

4. **Port 8000 مستخدم؟**
   - استخدم: `php artisan serve --port=8001`

---

## ✅ التحقق من التثبيت

بعد تشغيل `php artisan serve`، يجب أن ترى:
```
INFO  Server running on [http://127.0.0.1:8000]
```

افتح المتصفح وانتقل إلى العنوان أعلاه.

