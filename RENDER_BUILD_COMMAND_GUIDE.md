# 📍 أين تكتب Build Command في Render؟

## 🎯 الموقع بالتفصيل:

### الخطوة 1: إنشاء Web Service
1. اذهب إلى [dashboard.render.com](https://dashboard.render.com)
2. اضغط على **"New +"** (أعلى اليسار)
3. اختر **"Web Service"**

### الخطوة 2: ربط Repository
1. اختر **"Build from a Git repository"**
2. اربط حساب GitHub (إذا لم يكن مربوطاً)
3. اختر Repository المشروع
4. اضغط **"Connect"**

### الخطوة 3: إعدادات الخدمة

ستظهر لك صفحة إعدادات الخدمة. ابحث عن:

#### Basic Settings (أعلى الصفحة):
- **Name:** `law-firm-app`
- **Region:** اختر الأقرب لك
- **Branch:** `main`
- **Root Directory:** (اتركه فارغاً)

#### ⚠️ Build & Deploy Settings (هنا Build Command!)

قم بالتمرير لأسفل حتى تجد قسم **"Build & Deploy"**:

```
┌─────────────────────────────────────────┐
│ Build & Deploy                          │
├─────────────────────────────────────────┤
│ Runtime: [PHP ▼]                        │
│                                         │
│ Build Command:                          │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │ (هنا تكتب Build Command)            │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ Start Command:                          │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │ (هنا تكتب Start Command)            │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

---

## 📝 Build Command (انسخ والصق):

```bash
composer install --no-dev --optimize-autoloader && npm ci && npm run build && php artisan config:cache && php artisan route:cache && php artisan storage:link
```

**⚠️ ملاحظة:** تم إزالة `view:cache` لأنه يسبب خطأ "View path not found" في بعض الحالات.

---

## 🚀 Start Command (انسخ والصق):

```bash
php artisan serve --host=0.0.0.0 --port=$PORT
```

---

## 📸 صورة توضيحية للواجهة:

```
Render Dashboard
├── New + (أعلى اليسار)
│   └── Web Service
│       └── Build from Git repository
│           └── [اختر Repository]
│               └── Connect
│                   └── [صفحة الإعدادات]
│                       ├── Basic Settings
│                       │   ├── Name
│                       │   ├── Region
│                       │   └── Branch
│                       │
│                       └── Build & Deploy ⬅️ هنا!
│                           ├── Runtime: PHP
│                           ├── Build Command ⬅️ هنا تكتب!
│                           └── Start Command ⬅️ هنا تكتب!
```

---

## ✅ خطوات سريعة:

1. **New +** → **Web Service**
2. اختر **Repository** من GitHub
3. اضغط **"Connect"**
4. املأ **Basic Settings**
5. **قم بالتمرير لأسفل** إلى **"Build & Deploy"**
6. اكتب **Build Command** في الحقل المخصص
7. اكتب **Start Command** في الحقل المخصص
8. اضغط **"Create Web Service"**

---

## 💡 نصائح:

- **Build Command** هو حقل نصي كبير (textarea)
- يمكنك كتابة أوامر متعددة مفصولة بـ `&&`
- تأكد من عدم وجود أخطاء إملائية
- يمكنك الضغط على **"Advanced"** لإعدادات إضافية

---

## 🔍 إذا لم تجد Build Command:

1. تأكد من أنك في صفحة **"Create Web Service"**
2. قم بالتمرير لأسفل - قد يكون مخفياً
3. تأكد من اختيار **Runtime: PHP**
4. إذا كنت تستخدم **Blueprint** (render.yaml)، فلن تحتاج لكتابة Build Command يدوياً

---

## 📋 ملخص:

**Build Command** موجود في:
- صفحة إنشاء Web Service
- قسم **"Build & Deploy"**
- تحت **"Runtime"**
- حقل نصي كبير

**الموقع الكامل:**
```
Dashboard → New + → Web Service → Connect Repository → Build & Deploy → Build Command
```

---

## 🆘 إذا واجهت مشكلة:

1. تأكد من أنك في صفحة **"Create Web Service"** وليس **"Create Static Site"**
2. اختر **Runtime: PHP** (ليس Node أو Python)
3. إذا كنت تستخدم **render.yaml**، يمكنك تخطي هذا - سيتم قراءة الإعدادات من الملف تلقائياً

